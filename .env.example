# OfferFlow does not require build-time secrets.
# Configure an OpenAI-compatible Provider and API Key in the app's Settings page.
# Never commit a real API Key here: Vite build-time variables are visible to browsers.
