# OfferFlow v1 开发验收记录

更新日期：2026-08-24

## 自动化验证

在本地执行：

```bash
npm test
npm run test:e2e
npm run build
```

当前结果：

- 7 个 Vitest 测试文件通过；
- 18 个单元/集成/组件测试通过；
- 7 个 Playwright 浏览器 E2E 场景通过；
- TypeScript 类型检查通过；
- Vite 生产构建通过；
- 生产构建使用相对资源路径和 Hash Router，适配 GitHub Pages 子路径与静态服务器。

## 验收场景覆盖

| PRD 场景 | 当前证据 |
|---|---|
| AC-01 首次使用 | 组件验收测试 + 浏览器实际创建招聘季 |
| AC-02 招聘季隔离 | 自动化测试验证同公司、同岗位名在不同招聘季生成独立 Position 与 JD |
| AC-03 JD 提纯 | 浏览器 E2E 使用模拟兼容接口验证完整 UI 流程；另有 Schema、JSON 输出校验和 `response_format` 不兼容回退测试 |
| AC-04 创建投递 | 自动化测试验证默认 Pipeline 与 created History |
| AC-05 自定义 Pipeline | 自动化测试验证插入阶段、改名及历史快照不变；编辑器支持拖拽和键盘排序 |
| AC-06 Pipeline 复制 | 自动化测试验证只复制阶段定义并生成新 ID |
| AC-07 看板统计 | Dashboard 按固定 PipelineCategory 统计，具体阶段仍显示在卡片内 |
| AC-08 HR 位置变化 | HR 面映射为 interview，大类与顺序相互独立 |
| AC-09 笔试日程 | 组件验收测试 + 浏览器实际验证本周、月历和投递日程同步出现 |
| AC-10 面经 | 每场 Interview 独立保存，支持 Markdown、筛选与搜索 |
| AC-11 官网密码 | 自动化测试验证独立存储、AES-GCM 密文和多登录方式元数据 |
| AC-12 普通导出 | 自动化测试验证密码、账号 Secret、API Key 和加密密钥不进入导出 |
| AC-13 导入恢复 | 自动化测试验证导出后全量导入及引用关系完整性 |
| AC-14 LLM 未配置 | 浏览器 E2E 验证点击 AI 提纯后跳转设置页，不产生未捕获异常 |
| AC-15 GitHub Pages | 生产构建、Hash 路由刷新、资源请求与线上 GitHub Pages 均已验证通过 |

## 浏览器手工验证

已在本地浏览器实际完成：

1. 首次打开与创建招聘季；
2. 新增公司和岗位；
3. 保存 JD 原文与结构化 JD；
4. 从岗位开始投递；
5. 推进到笔试并检查历史快照；
6. 创建带时间的笔试日程；
7. 验证日程同时出现在本周事项、需要关注和月历；
8. 使用全局搜索命中结构化 JD；
9. 检查主要页面中文文案与可访问名称；
10. 检查新浏览器会话无运行时错误。

## GitHub 状态

- `main` 分支持续集成已经成功运行；
- `main` 分支持续集成已经成功运行；
- 自定义“发布 GitHub Pages”工作流已经成功构建并部署 `dist/`；
- Pages 发布源已切换为 GitHub Actions；
- 正式地址 `https://gqrcherry.github.io/OfferFlow/`、HTML 入口、JavaScript 和 CSS 资源均返回 200；
- 已用浏览器打开正式地址，首屏正常渲染且无控制台错误；
- Pages 正式 URL、HTML、JavaScript 与 CSS 均返回 200。

## Definition of Done 审计

| 要求 | 证据 | 状态 |
|---|---|---|
| 多招聘季管理与归档 | 招聘季 CRUD、归档只读、恢复与级联删除 | 通过 |
| Position/JD 招聘季隔离 | AC-02 单元与 E2E | 通过 |
| 岗位库 CRUD | 浏览器操作、筛选、排序、地点过滤、分页 | 通过 |
| 投递看板与列表 | 固定五大类看板、筛选、日期范围、排序、分页/分批加载 | 通过 |
| 自定义 Pipeline 与中途新增轮次 | 拖拽/键盘排序、插入主管面 E2E | 通过 |
| Pipeline History | created/stage/result/insert 快照与不可删除历史阶段保护 | 通过 |
| 日程进入本周与月历 | AC-09 E2E | 通过 |
| 每场面试独立面经并可搜索 | Interview 模型、筛选、Markdown、搜索测试 | 通过 |
| 招聘官网账号显示复制 | AES-GCM 独立 Secret store、显示/复制 E2E | 通过 |
| Secret 排除 | 普通导出和搜索隐私测试 + E2E 文件内容校验 | 通过 |
| JD Raw 与 AI 提纯 | Raw 隐藏、OpenAI-compatible 抽象、SiliconFlow preset、AI E2E | 通过 |
| JSON round-trip 与 CSV | 导入事务、引用校验、恢复 E2E、中文 CSV | 通过 |
| 主题与静态部署 | system/light/dark、Hash Router、生产静态预览 | 通过 |
| GitHub Pages | 正式地址和资源 200、浏览器首屏无错误 | 通过 |
| README / SECURITY / License | 中文文档与 MIT License | 通过 |
| 无后端依赖 | 仅 IndexedDB / Web Crypto / 浏览器 REST 调用 | 通过 |
